const numeros = [20, 50, 55, 3, 40, 100]

const nomes = ['JOSÉ', 'MARIA', 'JOÃO']

const misturado = [...numeros, ...nomes]

// Atribuir as variaveis n1, n2, n3, n4, n5, e n6 ao array numeros

const pessoa = {
  nome: 'Raphael',
  sobrenome: 'Rodrigues',
  idade: 32
}

// Atribuir as variaveis nome, sobrenome e idade ao objeto pessoa
